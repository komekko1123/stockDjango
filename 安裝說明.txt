conda env create --file environment.yml可以把所有文件放入，大概會安裝所有套件，然後會遇到的問題是ai的model的問題
在linux上可以裝anaconda或是我裝的這個miniconda(輕量的anaconda)，
安裝方法1.curl https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh --output anaconda.sh (裝miniconda)
2.mv anaconda.sh /tmp
3.cd /tmp4.bash anaconda.sh

要解決的問題通常會有後端神經網路keras選擇的是theano要開python把全域變數變成tensorflow才對
import os
os.environ['KERAS_BACKEND'] = 'tensorflow'

主要djnago的部分在stock_predict、MRT
conda env create --file environment.yml(創環境)，不行的話可能就只能開yml慢慢裝了。

conda activate cchh
cd到stockDjango目錄
python manage.py runserver 0.0.0.0:8000

ip地址:8000/stock_predict/home/ (第一次進去因為資料不是最新的可能會跑ai的錯誤要在f5一次等他跑完)
ip地址:8000/MRT/